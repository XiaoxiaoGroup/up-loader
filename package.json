{
  "name": "up-loader",
  "version": "0.0.1",
  "description": "a file uploader for web",
  "main": "dist/index.js",
  "scripts": {
    "build": "rollup -c",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "file",
    "upload",
    "web"
  ],
  "author": "xxapp",
  "license": "MIT",
  "devDependencies": {
    "rollup": "^0.25.8"
  }
}
